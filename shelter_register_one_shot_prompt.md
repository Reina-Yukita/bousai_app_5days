# 避難所登録機能を実装するための一括プロンプト

あなたはこのプロジェクトの既存構成を読み取り、他の機能を壊さずに「避難所登録」機能だけを完成させる実装担当AIです。実装前に関連するルート、データ保存処理、テンプレート、静的ファイル、認証処理を確認してください。既存のフレームワーク、命名規則、データ形式を優先し、不要な全面リファクタリングは行わないでください。

## 目的

避難所管理者が、登録方法を選択し、避難所情報を入力または修正し、登録前の確認画面を経て保存できるページを実装してください。

対象は避難所登録機能のみです。避難所検索、指示ボード、気象情報、ログインなど既存の別機能の仕様や見た目を変更しないでください。ただし、既存の認証処理や共通レイアウトを利用するために必要な最小限の変更は許可します。

## 必須の画面遷移

以下の順序で動作させてください。

1. `/shelter_register` を開く
2. 「新規登録」または「修正・変更」を選択する
3. 新規登録の場合は入力画面へ移動する
4. 修正・変更の場合は避難所名の検索画面へ移動する
5. 修正対象を選ぶと、現在の登録内容が入力画面に初期値として表示される
6. 入力画面から「確認へ」を押す
7. 確認画面に入力内容を表示する
8. 「修正する」で入力画面へ戻れる
9. 「登録する」で新規追加または既存情報の更新を行う
10. 保存完了画面を表示する

登録前に確認画面を必ず表示してください。入力画面から直接保存してはいけません。

## 新規登録・修正画面の入力項目

以下の項目を実装してください。

- 避難所名: 文字入力、必須
- 避難所町名: 文字入力、必須
- 住所: 文字入力、必須
- 電話番号: 数値入力、必須
- 地図画像: 画像ファイルアップロード
- 最大収容人数: 数値入力、0以上、必須
- 収容状況: 0%から100%まで10%単位のプルダウン、必須
- 設備・特徴: 以下のチェックボックス
  - バリアフリー
  - ペット可
  - 空調完備
  - 授乳室
  - 駐車場有
- 物資の在庫数: 以下のラジオボタンから1つ選択、必須
  - 十分足りている
  - 足りている
  - 足りていない
  - 全く足りていない

入力値はサーバー側でも検証してください。必須項目の不足、電話番号または最大収容人数の数値不正、収容状況の不正値を保存しないでください。

## 修正・変更の仕様

- 修正・変更を選ぶと、避難所名検索画面を表示する
- 検索語を含む避難所名を検索できる
- 検索結果から対象避難所を1件選択できる
- 選択した避難所の現在データを入力画面の初期値として表示する
- 画像をアップロードし直さなかった場合は、既存の地図画像を保持する
- 確認画面を経て、対象のIDのレコードを更新する
- 新しいレコードを追加してしまわないこと
- 対象が存在しない場合は、検索画面へ戻すかエラーを表示する

## データモデル

既存のデータ保存形式がある場合はそれに合わせてください。最低限、各避難所を次の形で扱えるようにしてください。

```json
{
  "id": 1,
  "name": "避難所名",
  "district": "避難所町名",
  "address": "住所",
  "phone": "電話番号",
  "map_image": "/static/uploads/map_example.png",
  "capacity": "100",
  "occupancy": "30",
  "features": ["バリアフリー", "空調完備"],
  "supplies": "足りている"
}
```

- `id` は既存IDと重複させない
- 新規登録時は既存最大ID + 1を使う
- 既存のJSON、DB、ORMなどの保存方法がある場合はそれを使う
- 保存に失敗した場合はエラーを表示し、成功したように見せない

## 画像アップロード

- `multipart/form-data` を使う
- 許可する拡張子は `.jpg`, `.jpeg`, `.png`, `.gif`, `.webp`
- 元のファイル名をそのまま保存せず、安全なファイル名を生成する
- 静的ファイル用ディレクトリ配下の uploads などに保存する
- 保存した画像URLを避難所データに保存する
- 修正時に新しい画像がない場合は既存URLを維持する
- 許可されない拡張子は保存せず、エラーを表示する

## UI要件

登録トップは、以下の2つを明確な大きめの選択カードとして表示してください。

- 新規登録
- 修正・変更

入力画面は、次のまとまりで見やすく分けてください。

- 基本情報
- 収容情報
- 設備・特徴
- 物資の在庫数

確認画面では、次の内容を読み取り専用で表示してください。

- 避難所名
- 避難所町名
- 住所
- 電話番号
- 地図画像のプレビューまたは未登録表示
- 最大収容人数
- 収容状況
- 設備・特徴
- 物資の在庫数

戻るボタン、確認へボタン、修正するボタン、登録するボタンを用意してください。スマートフォンでも入力欄、ボタン、確認内容が横にはみ出さないレスポンシブ表示にしてください。

共通CSSを読み込める構成の場合は、全ページ共通テンプレートの `<head>` に次の1行を追加してください。既にある場合は重複追加しないでください。

```html
<link rel="stylesheet" href="{{ url_for('static', filename='style.css') }}">
```

`static/style.css` が存在しない場合は作成してください。既存の共通CSSがある場合は、既存デザインを壊さず、避難所登録画面に必要なスタイルを追加してください。

## 推奨するルート構成

既存のルーティング方式に合わせて、次のように責務を分けてください。

- `/shelter_register`: 新規登録／修正・変更の選択
- `/shelter_register/edit_search`: 避難所名検索
- `/shelter_register/form/new`: 新規登録入力
- `/shelter_register/form/edit`: 修正・変更入力
- `/shelter_register/complete`: 確認内容の保存

ルート名やURL形式を変更する必要がある場合は、既存プロジェクトの規約に合わせて構いません。ただし、画面の段階を選択、検索、入力、確認、完了に分離してください。

## 認証

既存システムに管理者認証がある場合、避難所の登録、修正、検索、保存のすべてに既存の認証保護を適用してください。未ログインの場合は既存ログイン画面へ遷移させてください。

## 実装手順

1. 既存の避難所データ構造と保存処理を確認する
2. 登録に関係する既存ルートとテンプレートを確認する
3. 必要な共通ヘルパーを追加する
4. 選択、検索、入力、確認、保存完了の画面を実装する
5. 画像アップロードを実装する
6. 既存の別機能に影響がないことを確認する
7. 構文チェックまたは型チェックを実行する
8. テストまたはHTTPスモークテストを実行する

## 必須の確認項目

実装後、次を確認してください。

- 登録トップが表示できる
- 新規登録を選ぶと空の入力画面が表示される
- 必須項目が空の場合に保存されない
- 電話番号と最大収容人数の不正値を保存しない
- 収容状況が10%単位で選択できる
- 設備・特徴を複数選択できる
- 物資の在庫数を1つ選択できる
- 入力後に確認画面が表示される
- 確認画面から入力画面へ戻れる
- 新規登録で新しいIDのレコードが追加される
- 修正・変更で既存レコードが更新される
- 修正時に登録済みデータが初期表示される
- 地図画像を保存・表示できる
- 修正時に画像を変更しなければ既存画像が残る
- 保存完了画面が表示される
- スマートフォン幅でも表示が崩れない

## 完了条件

実装、検証、必要な修正まで自律的に行ってください。最後の回答では、変更したファイル、実装した機能、実行した検証コマンド、未解決の問題があればその内容を簡潔に報告してください。コードを書かずに計画だけを返して終了してはいけません。
